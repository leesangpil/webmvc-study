server:
  port: 8080

spring.h2.console:
  enabled: true
  path: /h2-console

security:
  oauth2:
    client:
      client-id: foo
      client-secret: bar
    authorization:
      check-token-access: isAuthenticated()